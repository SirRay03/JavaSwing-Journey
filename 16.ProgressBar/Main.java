public class Main {
    public static void main(String[] args){
        // JProgressBar = GUI component that visually displays the progress of some task
        //                has a horizontal & vertical version
        //                by default, it's horizontal
        //                minimum, maximum & current value
        //                can be changed using the "setValue()" method
        //                can be changed using the "setStringPainted()" method
        //                can be changed using the "setString()" method
        //                can be changed using the "setOrientation()" method
        //                can be changed using the "setForeground()" method
        //                can be changed using the "setBackground()" method
        //                can be changed using the "setBorder()" method
        //                can be changed using the "setBorderPainted()" method
        //                can be changed using the "setStringPainted()" method
        
        ProgressBarDemo demo = new ProgressBarDemo();
    }
}
